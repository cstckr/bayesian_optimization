# conda env create --file env.yml --force
name: bayesian_opt
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pygmo=2.19.7
  - optuna=3.6.1
  - optuna-integration[botorch]
  - pip:
    - torch==2.3.0
    - gpytorch==1.12
    - botorch==0.11.3
    - pandas==2.2.2
    - matplotlib==3.9.2